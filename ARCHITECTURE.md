# 🔄 Application Flow & Architecture

## 📱 User Journey

```
┌─────────────────────────────────────────────────────────┐
│                    USER OPENS APP                        │
│                  (http://localhost:3000)                 │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
            ┌───────────────────────┐
            │  Is User Logged In?   │
            └───────────┬───────────┘
                        │
            ┌───────────┴───────────┐
            │                       │
           NO                      YES
            │                       │
            ▼                       ▼
    ┌───────────────┐      ┌──────────────┐
    │  Login Page   │      │  Social Page │
    │  /login       │      │  /social     │
    └───────┬───────┘      └──────┬───────┘
            │                     │
            │                     ▼
            │         ┌───────────────────────┐
            │         │  View Feed            │
            │         │  • All Posts          │
            │         │  • My Posts (Tab)     │
            │         └──────────┬────────────┘
            │                    │
            ▼                    ▼
    ┌──────────────┐    ┌───────────────────┐
    │  Signup Page │    │  Create Post      │
    │  /signup     │    │  • Text           │
    └──────┬───────┘    │  • Image          │
           │            │  • Both           │
           │            └─────────┬─────────┘
           │                      │
           └──────────┬───────────┘
                      │
                      ▼
              ┌───────────────┐
              │  Interactions │
              │  • Like       │
              │  • Comment    │
              │  • Delete     │
              └───────────────┘
```

---

## 🏗️ System Architecture

```
┌────────────────────────────────────────────────────────────┐
│                        FRONTEND                             │
│                   (React.js on Port 3000)                   │
│                                                             │
│  ┌────────────┐  ┌────────────┐  ┌─────────────┐         │
│  │   Login    │  │   Signup   │  │   Social    │         │
│  │   Page     │  │   Page     │  │   Feed      │         │
│  └────────────┘  └────────────┘  └─────────────┘         │
│                                                             │
│  ┌──────────────────────────────────────────────┐         │
│  │         Components                           │         │
│  │  • CreatePost  • PostCard  • PrivateRoute   │         │
│  └──────────────────────────────────────────────┘         │
│                                                             │
│  ┌──────────────────────────────────────────────┐         │
│  │         Services (API Layer)                 │         │
│  │  • authAPI  • postsAPI  • axios config      │         │
│  └────────────────────┬─────────────────────────┘         │
└───────────────────────┼──────────────────────────────────┘
                        │
                        │ HTTP Requests
                        │ (JWT Token in Headers)
                        │
                        ▼
┌────────────────────────────────────────────────────────────┐
│                         BACKEND                             │
│                   (Express.js on Port 5000)                 │
│                                                             │
│  ┌───────────────────────────────────────────────┐        │
│  │              API ROUTES                       │        │
│  │                                               │        │
│  │  /api/auth/signup    ─── POST                │        │
│  │  /api/auth/login     ─── POST                │        │
│  │  /api/posts          ─── GET, POST           │        │
│  │  /api/posts/:id      ─── GET, DELETE         │        │
│  │  /api/posts/:id/like ─── POST                │        │
│  │  /api/posts/:id/comment ─ POST               │        │
│  └───────────────────┬───────────────────────────┘        │
│                      │                                     │
│  ┌──────────────────┴────────────────────────┐           │
│  │           MIDDLEWARE                      │           │
│  │  • auth.js     (JWT verification)         │           │
│  │  • upload.js   (Multer file upload)       │           │
│  └──────────────────┬────────────────────────┘           │
│                     │                                     │
│  ┌──────────────────┴────────────────────────┐           │
│  │            MODELS                         │           │
│  │  • User.js    (User schema)               │           │
│  │  • Post.js    (Post schema)               │           │
│  └──────────────────┬────────────────────────┘           │
└────────────────────┼──────────────────────────────────────┘
                     │
                     │ Database Queries
                     │ (Mongoose ODM)
                     │
                     ▼
┌────────────────────────────────────────────────────────────┐
│                        DATABASE                             │
│                   (MongoDB on Port 27017)                   │
│                                                             │
│  ┌──────────────────┐         ┌─────────────────┐         │
│  │  users Collection│         │ posts Collection│         │
│  ├──────────────────┤         ├─────────────────┤         │
│  │ • _id            │         │ • _id           │         │
│  │ • username       │         │ • user (ref)    │         │
│  │ • email          │         │ • username      │         │
│  │ • password (hash)│         │ • text          │         │
│  │ • profilePicture │         │ • image         │         │
│  │ • createdAt      │         │ • likes[]       │         │
│  └──────────────────┘         │ • likesUsernames│         │
│                               │ • comments[]    │         │
│                               │ • createdAt     │         │
│                               └─────────────────┘         │
└────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│ User Signup  │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────┐
│ Frontend sends:                   │
│ • username                        │
│ • email                           │
│ • password                        │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Backend validates:                │
│ • Email format                    │
│ • Password length (min 6)         │
│ • Username length (min 3)         │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Backend checks:                   │
│ • Email not already registered    │
│ • Username not taken              │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Backend creates user:             │
│ • Hash password (bcrypt)          │
│ • Save to MongoDB                 │
│ • Generate JWT token              │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Backend returns:                  │
│ • User data                       │
│ • JWT token                       │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Frontend stores:                  │
│ • Token in localStorage           │
│ • User data in Context            │
│ • Sets Authorization header       │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Redirect to Social Feed           │
│ User is now authenticated         │
└───────────────────────────────────┘
```

---

## 📝 Create Post Flow

```
┌─────────────────────┐
│ User writes post    │
│ • Types text        │
│ • Selects image     │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────────────────┐
│ Validation:                      │
│ • At least text OR image         │
│ • Image size < 5MB               │
│ • Image type: jpg/png/gif        │
└─────────┬───────────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│ Frontend creates FormData:       │
│ • text (if provided)             │
│ • image file (if provided)       │
└─────────┬───────────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│ POST /api/posts                  │
│ • Authorization: Bearer <token>  │
│ • Content-Type: multipart/form   │
└─────────┬───────────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│ Backend authenticates:           │
│ • Verify JWT token               │
│ • Extract user ID                │
└─────────┬───────────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│ Backend handles upload:          │
│ • Multer saves image             │
│ • Validates file type/size       │
└─────────┬───────────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│ Backend creates post:            │
│ • user: userID                   │
│ • username: from token           │
│ • text: from body                │
│ • image: file path               │
│ • Save to MongoDB                │
└─────────┬───────────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│ Backend returns post data        │
└─────────┬───────────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│ Frontend updates UI:             │
│ • Add post to feed (top)         │
│ • Clear form                     │
│ • Show success message           │
└──────────────────────────────────┘
```

---

## ❤️ Like/Comment Flow

```
┌──────────────────┐
│ User clicks Like │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────┐
│ POST /api/posts/:id/like         │
│ • Authorization: Bearer <token>  │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Backend checks if already liked: │
│ • Find post by ID                │
│ • Check if userID in likes[]     │
└────────┬────────────────────────┘
         │
    ┌────┴────┐
    │         │
  YES        NO
    │         │
    ▼         ▼
┌───────┐ ┌────────┐
│Unlike │ │  Like  │
│Remove │ │  Add   │
│userID │ │ userID │
└───┬───┘ └───┬────┘
    │         │
    └────┬────┘
         │
         ▼
┌─────────────────────────────────┐
│ Save post to database            │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Return updated post data:        │
│ • likes: [userIDs]               │
│ • likesCount                     │
│ • likesUsernames                 │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Frontend updates UI:             │
│ • Toggle heart icon color        │
│ • Update likes count             │
│ • Instant feedback               │
└──────────────────────────────────┘
```

---

## 🗃️ Data Models

### User Model
```javascript
{
  _id: ObjectId("507f1f77bcf86cd799439011"),
  username: "johndoe",
  email: "john@example.com",
  password: "$2a$10$N9qo8uLOickgx2ZMRZoMye...", // hashed
  profilePicture: "",
  createdAt: ISODate("2025-11-07T10:00:00Z")
}
```

### Post Model
```javascript
{
  _id: ObjectId("507f1f77bcf86cd799439012"),
  user: ObjectId("507f1f77bcf86cd799439011"),
  username: "johndoe",
  text: "Hello World! This is my first post 🎉",
  image: "/uploads/post-1699350000000-123456789.jpg",
  likes: [
    ObjectId("507f1f77bcf86cd799439013"),
    ObjectId("507f1f77bcf86cd799439014")
  ],
  likesUsernames: ["janedoe", "bobsmith"],
  comments: [
    {
      _id: ObjectId("507f1f77bcf86cd799439015"),
      user: ObjectId("507f1f77bcf86cd799439013"),
      username: "janedoe",
      text: "Great post!",
      createdAt: ISODate("2025-11-07T10:15:00Z")
    }
  ],
  createdAt: ISODate("2025-11-07T10:00:00Z")
}
```

---

## 🔄 State Management

### Frontend State (React Context)
```javascript
AuthContext:
  - user: { _id, username, email }
  - isAuthenticated: boolean
  - login(userData, token)
  - logout()

Component State:
  - Social Page:
    - posts: []
    - loading: boolean
    - error: string
    - page: number
    - hasMore: boolean
  
  - PostCard:
    - showComments: boolean
    - commentText: string
    - submittingComment: boolean
```

---

## 📡 API Request/Response Examples

### Signup
```http
POST /api/auth/signup
Content-Type: application/json

{
  "username": "johndoe",
  "email": "john@example.com",
  "password": "password123"
}

Response 201:
{
  "_id": "507f1f77bcf86cd799439011",
  "username": "johndoe",
  "email": "john@example.com",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### Create Post
```http
POST /api/posts
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: multipart/form-data

text: "Hello World!"
image: [binary file data]

Response 201:
{
  "_id": "507f1f77bcf86cd799439012",
  "user": "507f1f77bcf86cd799439011",
  "username": "johndoe",
  "text": "Hello World!",
  "image": "/uploads/post-1699350000000-123456789.jpg",
  "likes": [],
  "likesUsernames": [],
  "comments": [],
  "createdAt": "2025-11-07T10:00:00Z"
}
```

### Get Posts
```http
GET /api/posts?page=1&limit=20

Response 200:
{
  "posts": [...],
  "currentPage": 1,
  "totalPages": 5,
  "totalPosts": 87
}
```

---

## 🎨 Component Hierarchy

```
App
├── AuthProvider (Context)
│   └── Router
│       ├── Login Page
│       │   └── Form Components
│       │
│       ├── Signup Page
│       │   └── Form Components
│       │
│       └── PrivateRoute
│           └── Social Page
│               ├── AppBar
│               │   ├── Title
│               │   ├── Points Badge
│               │   ├── Balance Badge
│               │   └── Action Icons
│               │
│               ├── Tabs (All Posts / My Posts)
│               │
│               ├── CreatePost Component
│               │   ├── Text Input
│               │   ├── Image Upload
│               │   └── Submit Button
│               │
│               ├── Posts Feed
│               │   └── PostCard (multiple)
│               │       ├── Header (Avatar, Username, Date)
│               │       ├── Content (Text/Image)
│               │       ├── Actions (Like, Comment, Share)
│               │       └── Comments Section
│               │           ├── Comment List
│               │           └── Comment Form
│               │
│               └── Bottom Navigation
```

---

## 🔒 Security Measures

1. **Password Security**
   - Bcrypt hashing (10 rounds)
   - Never stored in plain text
   - Never sent in responses

2. **Authentication**
   - JWT tokens (30-day expiry)
   - Token required for protected routes
   - Token stored in localStorage

3. **Authorization**
   - Users can only delete own posts
   - User ID extracted from JWT token
   - No user ID manipulation possible

4. **Input Validation**
   - Express-validator for all inputs
   - File type validation
   - File size limits
   - SQL injection prevention (Mongoose)

5. **CORS**
   - Configured for frontend origin
   - Prevents unauthorized API access

---

This architecture ensures a secure, scalable, and maintainable application! 🚀
